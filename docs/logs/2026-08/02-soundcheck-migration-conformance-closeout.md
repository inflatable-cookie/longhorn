# Soundcheck Migration Conformance Closeout

Date: 2026-08-02
Status: complete
Roadmap: g01.016
Card: 119

## Outcome

Soundcheck closes on the exact `display -> window` Longhorn composition at
`e8970aa212e9464f53264f58f6628cd98254fc30`. It selects 14 Rust and six
renderer packages. Layout, Surfaces, transfer, commands, history, bridge,
notifications, backing-surface, child-view, remote capabilities, and helper
Tauri windows are absent.

The refreshed private artifact receipt installs nine TypeScript and 18 Rust
artifacts outside workspace resolution for the three frozen consumer shapes.
It proves one Svelte runtime, locked/offline Rust graphs, exact generated
bindings, no sibling-source resolution, no consumer writes, and no package
publication.

## Matrices

The Card 119 verifier replayed Cards 115-118 and checked 22 named product and
Longhorn cases. It was removed 2026-08-10 — Longhorn no longer keeps
consumer-aware proofs — and the fixture is the retained evidence:

- fresh, legacy, interrupted, conflicting, restart, and locator-last storage
- protected restore, capture, flush, timeout, and two-second close policy
- settings registration, product validation, SQLite inventory, online digest,
  confirmation-bound restore, and visible corrupt backup state
- scan reconnect, same-active cancellation, retry, and terminal lineage
- helper launch contract, readiness, resize, focus, close, crash, stale
  generation, request gap, and bounded history

Soundcheck passes 318 Rust tests and 30 renderer tests. The isolated product
profile verifies 6 portable, 1 hosted, and 3 native products with 7/4/1 plans.

## Rollback

The pre-migration build at `f4544e621b8ba3f4558c6b068db1cb36d6ef161f`
reached a stable run loop against a freshly reset isolated product profile.
The semantic database digest stayed
`64bee46a2f239fce6f6113bb063c866c1bb32426b641a9df2ef6cb42d26e4a15`;
retained `settings.json` stayed
`ca3d163bab055381827226140568f3bef7eaac187cebd76878e0b63e9e442356`.
The profile reverified afterward. No dual write, silent fallback, cleanup, or
live-library mutation ran. The temporary worktree was removed.

## Native Qualification

The macOS acceptance bundle built and started through the isolated proof-root
bypass. The protected main window initialized hidden and restore converged on
attempt one. The computer-control bridge timed out before app enumeration, so
visible plugin-GUI clicks are environment-unmet, not claimed. Product-boundary
tests cover helper resize/focus/close/crash/stale-generation behavior. Windows,
Linux, and non-1x Signal editor scale remain unsupported.

## Next

Execute Card 120 after rechecking Split-shell's exact dirty-path overlap. Preserve
all unrelated docs work and admit only the minimal config/settings graph.
