# Command System Artifact Proof And Closeout

Date: 2026-07-30
Card: 061
Roadmap: g01.010

## Result

Clean Jetstream and Loophole consumers run from produced TypeScript and
private Rust inventory artifacts. The minimal graph resolves no optional
command-system edge. The rich graph proves native admission, durable-keymap
and Tauri composition, per-instance Svelte state, public Poodle palette and
keybinding rendering, and settings admission. g01.010 is complete.

## Artifact Evidence

TypeScript:

| Package | Version | SHA-256 |
| --- | --- | --- |
| `@inflatable-cookie/longhorn-core` | 0.1.0 | `b41c3585e48f8e538acaccc68209660e7be55962bdea0af443af924011fcc9f0` |
| `@inflatable-cookie/longhorn-settings` | 0.1.0 | `d2b7b78122097705a95dafc48ab37fef52d3d5211268351e5263e059bf24997b` |
| `@inflatable-cookie/longhorn-commands` | 0.1.0 | `87abb5c662ac332e6faeb6f3970ed61017869f4d1277058d66b88e3d2d095207` |

Exact Poodle artifact set:
`39f08c04fa2579ae709db412c28221c04f22b89f09e633cef93764e5d49f8c74`.

Private Rust inventory archives:

- `longhorn-core`
- `longhorn-config`
- `longhorn-settings`
- `longhorn-command`
- `longhorn-command-config`
- `longhorn-command-settings`
- `longhorn-tauri-command`

Every crate passes `cargo package --list --allow-dirty`. The exact roots
unpack into a clean temporary workspace and run offline. They are not
registry-normalized `.crate` artifacts because the private dependency family
is not published.

## Dependency And Capability Matrix

| Shape | Rust | TypeScript | Permissions |
| --- | --- | --- | --- |
| Jetstream | command, core | commands root | command read |
| Loophole | command, config, settings, command config/settings, Tauri command | commands, core, settings, commands `/svelte` and `/poodle` | command read/mutate, listen/unlisten |

Jetstream resolves no config host, settings, bridge, Tauri TypeScript, Svelte,
or Poodle package. Loophole resolves one Svelte 5.38.6 runtime and the exact
Poodle artifacts. Both locks contain no workspace alias or sibling source
resolution.

## Semantic Evidence

| Area | Result |
| --- | --- |
| Jetstream | one global context; `Meta+O` resolves `jetstream:file.open` to a local executor |
| Loophole identity | `loophole:transport.play` is identical across palette, keyboard, menu, help, and settings |
| native context | changed context rejects panel execution before the executor |
| native availability | changed product availability rejects transport execution before the executor |
| route mapping | panel close maps locally; transport play maps to a typed domain operation |
| browser focus | editable focus gates blocked transport input without consumption |
| conflict | equal-specificity distinct invocations stay unresolved |
| refresh race | an older availability completion cannot replace revision 3 |
| persistence | injected commit failure leaves the prior effective keymap object installed |
| teardown | catalogue, keymap, availability, and keyboard listeners return to zero |
| UI | one mounted rich proof uses public Poodle palette and keybinding controls |
| settings | native page admission requires catalogue and writable-keymap capabilities |

## Boundary Audits

- generated command bindings have zero drift
- shared Rust and TypeScript source contains no Loophole, Jetstream,
  Soundcheck, or Bovine command payload
- shared Tauri hosting has catalogue/keymap reads and checked mutation only
- no generic command execution endpoint or bridge bus exists
- command execution remains an injected consumer authority
- capability files match selected reads, mutation, and event listeners
- optional peers remain absent from the minimal install
- public Poodle exports are the only visual seam
- no donor repository was read for execution or written

## Behavior Delta

| Class | Result |
| --- | --- |
| retained | product command IDs and declarations, context facts, authorization, local and typed-domain behavior, labels, icons, shell framing |
| changed | sealed registry, search, physical keyboard rules, sparse overrides, checked host joins, projection and lifecycle mechanics |
| rejected | generic execute endpoint, renderer authority, implicit conflict order, silent persistence fallback, shared donor catalogues |
| deferred | donor cutover, public release, native accelerators, global hotkeys, multi-stroke input, macros, other triggers, automation, plugins, synchronized keymaps |

## Validation

- `effigy proof:command-system-artifacts`
- two clean TypeScript installs and executable traces
- two offline Rust consumer runs
- one isolated mounted Svelte/Poodle test
- exact package, peer, capability, payload, authority, route, and artifact
  audits
- full `effigy qa`

## Closeout

Cards 056-061 and g01.010 are complete. Northstar returns to the g01 runway.
The next planning gate is g01.011 history research: revalidate the existing
linear and forkable evidence against the completed bridge and command
foundations before compiling implementation cards.
