# Tauri Window Host And Lifecycle Compilation

Date: 2026-07-28
State: complete planning batch

## Outcome

- reassessed current Loophole, Nucleus, and Soundcheck Tauri host code
- checked local Tauri 2.10.3 and 2.11.2 API and Rust-version evidence
- promoted native observation, apply, event, reveal, and flush decisions into
  contract 009
- compiled `g01.004` into six dependency-ordered cards
- made Card 017 the sole ready lane
- preserved a packaged-proof checkpoint before Card 022

## Donor Evidence

Loophole supplies the advanced multi-window host:

- protected hidden `main` slot and explicit runtime retag bookkeeping
- consumer-defined dynamic windows
- logical outer-origin plus inner-size capture
- unmaximize, position, size, maximize apply ordering
- page-load plus positioned reveal
- programmatic move and close evidence
- multi-window geometry debounce and dynamic capability patterns

Nucleus supplies the no-Surface single-window host:

- hidden restore before show
- physical monitor fallback and work-area clamp
- move, resize, scale, and focus-loss capture
- coalesced persistence and bounded close flush
- operator-confirmed restart restore

Soundcheck supplies the minimal settings-integrated host:

- hidden restore and work-area fit
- outer-origin plus inner-size capture
- move, resize, scale, close, and destroy handling
- bounded writer acknowledgement

No donor repository was modified.

## Contract Deltas

- host snapshots fail complete instead of omitting unreadable managed windows
- scale conversion is checked and explicitly nearest-rounded
- mixed-scale origins require an injected whole-desktop mapper
- unavailable built-in status remains unknown; ambiguous primary matching fails
- labels stay transport handles; stable identity lives in a host registry
- dynamic creation policy is injected
- native apply is non-transactional and per-operation receipted
- failed same-window dependencies skip; independent windows continue
- readback determines convergence
- generation evidence is registered before mutation
- event origin uses generation and expected state, not time alone
- timing bounds and user-close behavior remain consumer policy
- persistence and flush use an injected sink
- reveal requires placement convergence and page readiness

## Compiled Runway

1. Card 017 — Tauri display and live-window observation
2. Card 018 — native operation execution
3. Card 019 — event attribution and settling
4. Card 020 — capture, reveal, and flush
5. Card 021 — composition, mock runtime, capabilities, and failures
6. Card 022 — packaged proof and closeout

Card 017 is ready. Later cards remain planned until their predecessor closes.
Card 022 also requires an explicit packaged-proof checkpoint.

## Limits

- non-macOS strong display evidence remains open
- runtime proof names only platforms actually executed
- consumer migrations remain `g01.014` onward
- layout, Surfaces, drag, TS/Svelte/Poodle, and product window definitions stay
  outside `g01.004`

## Validation

- roadmap, contract, research, log, and front-door links pass
- Effigy Northstar checks pass
- Effigy Doctor remains warning-only with zero errors

## Posture

`strict-ready`

## Next

Review and explicitly start Card 017. Do not auto-start native observation.
