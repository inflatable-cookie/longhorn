# 094 Nucleus Behavior, Authority, And Rollback Freeze

Status: complete; downstream admission stopped on public Poodle seam and operator gates
Owner: Tom
Roadmap: g01.014 batch 1
Governing refs: contracts 001, 003, 004, 009, 012-014, and 017;
`../../../architecture/nucleus-migration-map.md`
Depends on: Card 093
Auto-start next card: no

## Objective

Turn the read-only Nucleus audit into executable donor-shaped fixtures,
authority boundaries, and rollback evidence before any donor code changes.

## Repository Scope

- Longhorn: documentation, fixtures, and focused verification may change.
- Nucleus: read-only at the recorded baseline.
- Poodle: read-only public-seam verification only.

## Scope

- exact donor commit and worktree state
- desktop storage inventory and legacy-source classification
- native window restore, capture, flush, and fallback behavior
- project-keyed five-region panel documents and mutations
- renderer lifetime, drag, overlay, and project-switch behavior
- native Browser construction, navigation, data-store, capability, visibility,
  focus, cursor, close, and reuse policy
- product/shared authority split
- migration ordering, rollback boundaries, and overlapping-work policy

## Steps

1. Recheck Nucleus status, commit, active roadmaps, and Effigy selector graph.
2. Freeze focused storage, window, layout, renderer, and Browser fixtures in
   Longhorn without importing product payloads.
3. Record retained, changed, and deferred behavior with exact donor paths.
4. Prove the fixtures map to existing Longhorn APIs or name a real gap.
5. Verify Browser policy against the child-view adapter and Svelte lifetime.
6. Audit the public Poodle seam needed to replace private overlay discovery.
7. Record per-slice rollback inputs and source-retention requirements.
8. Reconcile Nucleus g05 layout acceptance and donor-write admission.

## Acceptance Criteria

- donor commit, branch, worktree state, and active Nucleus lane are exact
- five-region and project-switch behavior is represented without Surfaces
- combined `ui.json`, SQLite, snapshots, and drafts have typed target classes
- Browser policy covers every field in the canonical migration map
- private Poodle selector use is isolated and has a named public-seam gate
- each planned donor slice has one authority transfer and one rollback route
- no Nucleus or Poodle file changes
- every shared expectation maps to a current Longhorn contract and package

## Evidence Required

- donor path and behavior matrix
- checked fixture inventory and mapping report
- browser policy and capability audit
- storage/import and rollback matrix
- dirty-worktree and Nucleus-roadmap gate report
- focused Northstar validation

## Stop Conditions

- current donor behavior contradicts the promoted Longhorn contract
- Nucleus has overlapping uncommitted changes
- preserving Browser overlay behavior requires a private Poodle seam
- rollback requires dual-write or silent legacy fallback
- a product authority would have to move into Longhorn

## Next Task

Resolve the public Poodle overlay-geometry seam and Nucleus g05 operator
acceptance before promoting Card 095. Do not publish or modify either donor.

## Completion Evidence

- the migration receipt (retired 2026-08-10; in git history)
- verifier removed 2026-08-10 — Longhorn no longer keeps consumer-aware proofs; the recorded fixture is the retained evidence
- `../../../logs/2026-08/01-nucleus-behavior-authority-and-rollback-freeze.md`
- `effigy qa:northstar:g01-nucleus-card094-freeze`

The checked result is `pass_with_admission_gates`. Nucleus and Poodle remained
clean at their pinned commits. Preserving exact native-Browser overlay
intersection requires a Poodle-owned public geometry contract; the current
private selector cannot cross the migration boundary. Donor-write admission
therefore remains false.
