---
title: g16.109 Longhorn v0.3.0 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-09-05
updated: 2026-09-05
handoff_path: /Users/tom/.paseo/worktrees/3t4k8w5o/g16-109-longhorn-v030-adoption/docs/handoffs/20260905-092310-g16-109-longhorn-v030-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, poodle-adoption]
---

## What This Thread Was Doing

This is the Longhorn tier-1 lane of Poodle's `g16.109` consumer adoption wave.
The job is to pin public registry `@inflatable-cookie/poodle-core` and
`@inflatable-cookie/poodle-svelte` at exact `0.3.0`, move the Svelte adapter
peer with them, and move `crates/longhorn-poodle`'s `poodle-specs` git tag from
`v0.2.2` to `v0.3.0`. Then prove the adapter peer converges on one identity
and open one PR against Longhorn `main`.

## Why It Matters

Poodle `v0.3.0` is published. Longhorn is the foundation for the later
consumer lanes. Until this pin moves, every Longhorn proof still installs
`0.2.2`, and the Svelte adapter peer can skew from the root lock.

## Current State

- **Repository:** `git@github.com:inflatable-cookie/longhorn.git`
- **Planning branch:** `main`
- **Planning base commit:** `621a5d86c1fab4d729c3b9acf5078b6348309ac8`
- **Pushed main verification:** `HEAD` equalled `origin/main` at
  `621a5d86c1fab4d729c3b9acf5078b6348309ac8` before this handoff
- **Planning checkout:** primary checkout at
  `/Users/tom/Dev/projects/longhorn` is on `main`; do not edit it
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** Poodle card
  `docs/roadmaps/g16/109-v030-consumer-adoption-wave.md` on Poodle `main`;
  this tracked handoff is committed after the planning base
- **Worker branch:** `worker/g16.109-longhorn-v030-adoption`
- **Worker worktree:**
  `/Users/tom/.paseo/worktrees/3t4k8w5o/g16-109-longhorn-v030-adoption`
- **Worktree creation command:** launcher-provided Paseo worktree; do not
  create another
- **Required sibling worktree links:** none
- **Active spec lane:** none; consumer adoption of a published release
- **Roadmap milestone:** Poodle `g16.109` tier 1, Longhorn row
- **Ready cards, in order:** Poodle
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/109-v030-consumer-adoption-wave.md`
- **Allowed runway:** Longhorn Poodle declarations, locks, bounded
  compatibility fallout, current-checkpoint contract lines, and this handoff
- **Remaining card budget:** this one Longhorn lane
- **Coordinator agent ID:** manual / operator-dispatched worker
- **Delivery route:** one PR against Longhorn `main`; merge stays with the
  Poodle coordinator
- **Dispatch topology:** Poodle `g16.109` tier 1 also launches Underlay and
  Soundcheck Library in their own repositories
- **Parallel safety check:** no writes outside this Longhorn worktree; do not
  edit Poodle
- **Surfaces this lane owns:** root `package.json`;
  `packages/longhorn-poodle-svelte` peer and its boundary test; example
  Poodle pins; `crates/longhorn-poodle` `poodle-specs`; both GPUI prototype
  Poodle git tags (same-source invariant); regenerated Bun/Cargo locks;
  contracts 012/013 current checkpoint; `scripts/poodle-release.ts` header;
  `PAPERCUTS.md` only if an open Longhorn entry is actually closed by `0.3.0`;
  this handoff
- **Integration ownership:** Poodle wave closeout (`g16/README.md`,
  generation index, README adoption count) stays with the Poodle coordinator
- **Canonical refs:** Longhorn `AGENTS.md`; contracts 012 and 013; Poodle
  `docs/release-notes/0.3.0.md`; Poodle tag `v0.3.0` at
  `85609d941a208ff2f854e9f7c0e457089cc77d0e`; Poodle main promotion
  `e3c63e910`
- **Review oracle:** Poodle card `g16.109` table (one identity; breaks
  absorbed not shimmed; board green; papercuts closed honestly)
- **Model capability profile:** ordinary implementation
- **Worker provider/model identity:** Grok 4.6
- **Frontier-worker justification:** none
- **Tool/runtime restrictions:** public registry only; no sibling Poodle
  path, `file:`, or local override; no desktop/native/windowed proofs; no
  merge
- **Required validation:** `effigy tasks`, `effigy doctor`,
  `effigy --json deps status bun`, lock grep for one Poodle identity,
  narrow Poodle-related checks, `effigy qa`, `git diff --check`
- **PR base/head:** `main` ← `worker/g16.109-longhorn-v030-adoption`
- **PR URL:** pending
- **Review state:** awaiting worker PR
- **Merge path:** orchestrator after accepted review of the current head and
  passing required checks

## Boundaries

- **In scope:** exact public `0.3.0` pins wherever
  `@inflatable-cookie/poodle-core` and `@inflatable-cookie/poodle-svelte` are
  declared; Svelte adapter peer; `poodle-specs` tag `v0.3.0`; prototype Poodle
  git tags moved with it so one crate source remains; lock regeneration
  limited to Poodle; current-checkpoint contract lines; honest papercut
  closeout; one PR
- **Out of scope:** Poodle edits; sibling path resolution; Longhorn version
  bumps or publication; historical 0.2.2 prose that is not the live pin;
  desktop or native proofs; merge
- **Outcome shape:** complete pin-and-proof lane. Open a reviewable PR; do
  not merge
- Work only in the launcher worktree. Do not edit the primary checkout
- Stop if the bump exposes a Longhorn defect that is not compatibility
  fallout, if lock churn is unrelated, or if public registry resolution fails

## Important Context

- **Planning lineage:** Poodle `g16.097` published `0.3.0`. `g16.109` is the
  consumer wave. Longhorn's previous pin is public `0.2.2` (g16.008 / Longhorn
  PR 9)
- **Why these cards are ready:** npm `latest` is `0.3.0`; tag `v0.3.0` is
  `85609d941a208ff2f854e9f7c0e457089cc77d0e`; `poodle-specs` at that tag is
  version `0.3.0`
- **Decisions and preferences:** resolve from the public registry only. Use
  this repo's Bun `1.3.14` and Effigy selectors. Example Poodle pins and GPUI
  prototype tags move with the live pin so one identity holds; `gpui` 0.2.2
  on crates.io stays
- **Open tensions:** Longhorn `PAPERCUTS.md` has no open Poodle entries.
  Close only an entry the release actually fixes. HistoryCenter v3 fallout
  belongs to Loophole, not this lane
- **Report after:** one PR with lock/dependency evidence and the board
- **Report to:** the operator / owning coordinator. Do not merge

## Suggested Next Move

Reuse the launcher worktree. Read `AGENTS.md`, this handoff, the Poodle card,
and contracts 012/013. Pin `0.3.0`, regenerate locks, prove one identity, run
the board, then open the PR.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record
   its actual root/branch; do not create another worktree.
3. From the selected worktree, record this handoff's repository-relative path
   (`docs/handoffs/20260905-092310-g16-109-longhorn-v030-adoption.md`). Run
   `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Confirm the planning base is an ancestor of `HEAD`. After this file is
   committed, the tracked `HEAD` copy is canonical.
4. Required sibling worktree links are `none`; skip link creation.
5. Read the Poodle card, `AGENTS.md`, and contracts 012/013.
6. Run cheap orientation (`effigy tasks`, `effigy doctor`) and record it.

### While you work

- Keep commits aligned with one adoption chunk, not model turns.
- Stop before PR if a contract is missing, intent is ambiguous, or validation
  changes the plan. Send a complete 10-part capsule; do not ask the operator
  directly or open a placeholder PR.
- Do not invent architecture or shim removed Poodle exports.

### When the assigned runway is complete

1. Run `effigy qa` and `git diff --check`. Prove one Poodle identity from
   public registry resolution.
2. Falsify the diff against the card's review oracle.
3. Push `worker/g16.109-longhorn-v030-adoption` and open one PR against
   current `main`.
4. Return the PR URL, changed files, lock evidence, tests, and this handoff's
   absolute path. Do not merge.

### Review and merge path

Independent review of the exact PR head. Merge belongs to the orchestrator
after accepted review and passing checks.

- **Closeout refs:** this handoff; contracts 012/013 current checkpoint;
  Poodle `g16.109` card. Longhorn generation-index live pointer is not this
  lane's closeout.

### Handoff closeout

Leave the PR honest about residual historical `0.2.2` mentions and any
papercut that was not closed because it was not actually fixed in `0.3.0`.
