---
kind: northstar-handoff
title: "g02.026 stage 1 — Release hardening (Card 218 remainder)"
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Tom
created: 2026-09-15
updated: 2026-09-15
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Operator-confirmed direction in this conversation on 2026-09-15: unblock and finish the open g02 work, then cut the first release; dispatch through northstar-queue."
roadmap: docs/roadmaps/g02/026-automation-and-gate-convergence.md
tags: [coordination, handoff, worker, pr, g02, release-sequence, release-hardening]
---

## What This Thread Was Doing

The Longhorn planning thread recompiled g02.026. Cards 219 and 220 are complete;
Card 218's exemption-exit framing is gone with the linked-Poodle exemption. Three
items survive as release hardening and must pass before the first publish.

## Why It Matters

The first publish is close to irreversible (npm unpublish is a 72-hour window
and only while nothing depends on the package). These three checks protect the
release's core claim: that a consumer resolves the published packages, including
Poodle, from the registry, and that a green rehearsal is not masking a missing
dependency.

## Current State

- **Done:** Cards 219 and 220 complete (gate hygiene, supply-chain visibility);
  the linked-Poodle exemption is deleted (g16.008, step 5).
- **Still open:** the three release-hardening items in the canonical task's
  Stage 1.
- **Active spec lane:** none.
- **Current task:** `docs/roadmaps/g02/026-automation-and-gate-convergence.md`.
- **Canonical refs:** contract 012; memo 023.
- **Remaining continuation envelope:** none — this handoff covers the lane.
- **Lane budget / pause signal:** normal; stop on the task's stop conditions.
- **Required sibling worktree links:** none.
- **Key files:**
  - `/Users/tom/Dev/projects/longhorn/scripts/poodle-release.ts`
  - `/Users/tom/Dev/projects/longhorn/scripts/ci-rehearse.sh`
  - `/Users/tom/Dev/projects/longhorn/packages/longhorn-poodle-svelte/package.json`
  - `/Users/tom/Dev/projects/longhorn/effigy.toml`

## Boundaries

- **In scope:** `poodle-release.ts` verifies the Poodle integrity bytes rather
  than a version string; `ci-rehearse` detects and reports the bun global-link
  state; a pack-level typecheck of `longhorn-poodle-svelte` against the
  **registry** Poodle.
- **Out of scope:** publishing anything, and changing `.github/workflows/`
  trigger policy. Workflow edits need explicit human approval.
- **Repo constraints:** follow `/Users/tom/Dev/projects/longhorn/AGENTS.md`. Do
  not reintroduce a local Poodle shortcut to make a check pass.

## Important Context

- **Planning lineage:** memo 023's automation lane; Card 218 part-landed
  2026-08-24.
- **How the plan fits the system:** contract 012's distribution claims depend on
  this envelope being honest.
- **Decisions:** the items are release hardening, not an exemption exit. The
  stage-2 checkpoint (selector routing) is already met.
- **Open tensions:** the runner-environment trust question still applies — the
  pack-level typecheck must resolve Poodle from npm, not a sibling checkout, or
  it proves nothing.

### UI Design Brief

Not applicable. This is release tooling.

## Suggested Next Move

Read the canonical task's Stage 1, then start with `ci-rehearse` link-state
detection — it is the cheapest item and its evidence is easiest to reproduce on
a clean runner.

## Completion Protocol

You are the implementation worker for this lane. Work on the queue-owned branch,
not `main`.

1. Implement the three Stage 1 items and their acceptance checks.
2. Run `effigy ci:rehearse`, `effigy check:packages`, and the focused
   `poodle-release`/candidate selectors, then `effigy qa`.
3. Open a PR from the queue-owned branch and report `ready_for_review` through
   the Queue callback helper in your environment.
4. Fix review findings on the same branch; the reviewer resumes against your
   exact head.
5. After merge, closeout publishes the terminal record, roadmap status, and
   log. Do not edit roadmap indexes yourself.

Unresolved risk: if a Poodle-side gap blocks the registry typecheck, record it
and escalate rather than reintroducing a local shortcut.
