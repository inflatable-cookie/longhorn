# Tauri settings capability examples

Copy the selected permission and capability files into a consumer Tauri app.
The read-only capability grants registry/scope reads. The mutable capability
adds apply/reset. Both grant only event listen/unlisten; injected host
authorization remains required.
