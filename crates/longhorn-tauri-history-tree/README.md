# longhorn-tauri-history-tree

Narrow Tauri 2 handler assembly over a consumer-injected fork-history
authority. It exposes payload-free summaries, bounded default/alternate path
and branch pages, checked navigation, and non-durable revision hints.

The consumer retains caller authorization, product mutation, rollback, payload
meaning, storage, and recovery. Tauri capabilities grant reachability only.
