# Tauri transfer capability examples

Copy the selected capability and permission files into a consumer
`src-tauri` tree.

- `base-transfer` grants the five Surface-free transfer commands plus event
  listen/unlisten.
- `surface-transfer` adds only the two optional whole-Surface commands.

Replace the example window globs with the consumer's exact managed labels.
