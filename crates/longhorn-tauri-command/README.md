# longhorn-tauri-command

Narrow Tauri command assembly for an injected, caller-authorized command
catalogue and configuration-backed keymap authority.

The adapter exposes catalogue, keymap load, preview, commit, and reset only. It
contains no product command execution bus.
