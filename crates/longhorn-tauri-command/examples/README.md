# Tauri command capability examples

Copy the selected permission and capability files into a consumer Tauri app.
The read-only capability grants catalogue and keymap reads. The mutable
capability adds preview, commit, and reset. Both grant event listen/unlisten;
injected host authorization remains required.
