# longhorn-command-settings

Optional settings-registry declarations for shared keybinding navigation.

The crate owns no command execution, keymap persistence, renderer state, or
Poodle UI. Its page is admitted only when the host composes both command
catalogue and writable-keymap capabilities.
