{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "update-install",
  "description": "Stages a verified update and replaces the running application. Grant to the window that confirms it, and no other.",
  "windows": ["settings"],
  "permissions": [
    "allow-longhorn-update-install"
  ]
}
