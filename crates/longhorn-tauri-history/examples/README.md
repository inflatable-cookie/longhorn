# Capability examples

- `read-only-history.json`: snapshot/page plus event listening
- `mutable-history.json`: snapshot/page/navigation plus event listening

Capabilities control Tauri command reachability. The injected
`HistoryHostAuthority` still authorizes the caller and owns product apply.
