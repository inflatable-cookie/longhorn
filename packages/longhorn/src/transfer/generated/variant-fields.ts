// @generated by `effigy generate:transfer`; do not edit.
// Allowed field names per tagged-union variant, from the Rust enums.
// Keyed by type, then by discriminant. The discriminant itself is in
// each list, because `exact()` compares every key.

export const TRANSFER_VARIANT_FIELDS: Record<string, Record<string, readonly string[]>> = {
  "TransferTargetBinding": {
    "panel_region": ["kind", "host_binding_id", "document_id", "revision", "surface_id", "region_id"],
    "surface_window": ["kind", "host_binding_id", "document_id", "revision"],
  },
  "TransferCommitSelector": {
    "explicit_zone": ["kind", "drop_zone_id"],
    "screen_point": ["kind", "point"],
  },
  "TransferAbortSource": {
    "transfer": ["domain", "code"],
    "panel": ["domain", "code"],
  },
  "TransferSessionResponse": {
    "started": ["status", "session"],
    "aborted": ["status", "abort"],
  },
  "TransferLeaseResponse": {
    "published": ["status", "lease"],
    "aborted": ["status", "abort"],
  },
  "TransferCancelResponse": {
    "cancelled": ["status", "cancellation"],
    "aborted": ["status", "abort"],
  },
  "PanelTransferResponse": {
    "committed": ["status", "completion"],
    "aborted": ["status", "abort"],
  },
};

export const TRANSFER_VARIANT_FIELDS_DISCRIMINANTS: Record<string, string> = {
  "TransferTargetBinding": "kind",
  "TransferCommitSelector": "kind",
  "TransferAbortSource": "domain",
  "TransferSessionResponse": "status",
  "TransferLeaseResponse": "status",
  "TransferCancelResponse": "status",
  "PanelTransferResponse": "status",
};
