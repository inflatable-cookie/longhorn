// @generated by `effigy generate:config`; do not edit.
// Wording lives in Rust, on the enum that owns it. A label edited here is
// a label that disagrees with the native surface.

import type { RestoreAuthenticityProjection, RestoreDomainCompatibilityProjection, RestoreIntegrityProjection } from "./protocol.ts";

export const RESTORE_INTEGRITY_LABELS: Record<RestoreIntegrityProjection, string> = {
  verified: "Verified",
};

export const RESTORE_AUTHENTICITY_LABELS: Record<RestoreAuthenticityProjection, string> = {
  unauthenticated: "Unauthenticated",
  authenticated: "Authenticated",
};

export const RESTORE_COMPATIBILITY_LABEL_TEMPLATES: Record<RestoreDomainCompatibilityProjection["status"], string> = {
  ready: "Ready",
  migrationRequired: "Migration required ({from} → {to})",
  unknownDomain: "Unknown domain",
  descriptorMismatch: "Descriptor mismatch",
  domainCodeUnavailable: "Domain code unavailable",
  policyExcluded: "Policy excluded: {reason}",
  customAdapterUnavailable: "Adapter unavailable: {adapter}",
  customAdapterReady: "Custom adapter ready: {adapter}",
  customAdapterRejected: "Adapter rejected: {adapter} — {detail}",
  targetUnavailable: "Target unavailable: {reason}",
  sourcePreserved: "Source preserved: {issue}",
  sourceRejected: "Source rejected: {issue}",
  targetPreparationFailed: "Target preparation failed: {detail}",
};
