// @generated by `effigy generate:update`; do not edit.
// Allowed field names per tagged-union variant, from the Rust enums.
// Keyed by type, then by discriminant. The discriminant itself is in
// each list, because `exact()` compares every key.

export const UPDATE_VARIANT_FIELDS: Record<string, Record<string, readonly string[]>> = {
  "DeferralCause": {
    "userPostponed": ["cause"],
    "workInFlight": ["cause", "detail"],
    "installationNotWritable": ["cause", "detail"],
    "externallyManaged": ["cause", "manager", "command"],
    "installFailed": ["cause", "detail"],
  },
  "UpdateAvailabilityProjection": {
    "offer": ["state", "version", "reason", "notes"],
    "upToDate": ["state"],
    "aheadOfChannel": ["state", "installed", "channel"],
    "withheldByRollout": ["state", "version"],
    "managedElsewhere": ["state", "version", "manager"],
  },
  "UpdateProgressProjection": {
    "idle": ["state"],
    "downloading": ["state", "fraction"],
    "verifying": ["state"],
    "readyToInstall": ["state", "version"],
    "installing": ["state", "version"],
  },
  "UpdateInstallAuthorizationProjection": {
    "approved": ["status"],
    "deferred": ["status", "cause"],
  },
};

export const UPDATE_VARIANT_FIELDS_DISCRIMINANTS: Record<string, string> = {
  "DeferralCause": "cause",
  "UpdateAvailabilityProjection": "state",
  "UpdateProgressProjection": "state",
  "UpdateInstallAuthorizationProjection": "status",
};
