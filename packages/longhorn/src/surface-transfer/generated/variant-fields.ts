// @generated by `effigy generate:surface-transfer`; do not edit.
// Allowed field names per tagged-union variant, from the Rust enums.
// Keyed by type, then by discriminant. The discriminant itself is in
// each list, because `exact()` compares every key.

export const SURFACE_TRANSFER_VARIANT_FIELDS: Record<string, Record<string, readonly string[]>> = {
  "SurfaceTransferTarget": {
    "existing": ["kind", "target"],
    "provisioned": ["kind", "drop_point", "provisioning"],
  },
  "SurfaceTransferAbortSource": {
    "transfer": ["domain", "code"],
    "surface_transfer": ["domain", "code"],
  },
  "SurfaceSessionResponse": {
    "started": ["status", "session"],
    "aborted": ["status", "abort"],
  },
  "SurfaceTransferResponse": {
    "committed": ["status", "completion"],
    "aborted": ["status", "abort"],
  },
};

export const SURFACE_TRANSFER_VARIANT_FIELDS_DISCRIMINANTS: Record<string, string> = {
  "SurfaceTransferTarget": "kind",
  "SurfaceTransferAbortSource": "domain",
  "SurfaceSessionResponse": "status",
  "SurfaceTransferResponse": "status",
};
