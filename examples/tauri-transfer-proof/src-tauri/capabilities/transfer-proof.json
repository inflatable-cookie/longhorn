{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "transfer-proof",
  "description": "Minimal core capability for the packaged multi-window transfer proof.",
  "windows": ["source", "target", "provisioned"],
  "permissions": ["core:default"]
}
