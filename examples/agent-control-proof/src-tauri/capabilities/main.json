{
  "identifier": "agent-control-proof-main",
  "description": "The main window; the proof frontend needs no Tauri privileges — the control surface drives it from Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
