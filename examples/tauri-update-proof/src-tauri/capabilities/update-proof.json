{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "update-proof",
  "description": "The packaged update proof's only window, scoped to its own commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-update-proof-read",
    "allow-update-proof-drive"
  ]
}
